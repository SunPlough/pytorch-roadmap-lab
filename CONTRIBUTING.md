# Contributing

欢迎提交改进，尤其是：

- 新增一个能在 CPU 上快速跑完的小例子。
- 把一个常见 PyTorch 报错补进 `docs/06-debug-checklist.md`。
- 给练习增加更清晰的断言或提示。

提交前建议运行：

```bash
python -m pytest
python examples/03_train_toy_classifier.py
```

请不要提交大模型权重、数据集压缩包或训练日志目录。

